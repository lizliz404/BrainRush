# Progress Log

## 2026-03-28（体验修正与训练闭环补齐）

- 完成时间：2026-03-28 (UTC)
- 实现了哪些功能：
  - 调整 Word 题面：去掉“选择中文 / 选出英文”前缀，并为 HUD 题面与下落方块增加按内容长度缩字，避免单词题过于撑满。
  - 将 60 秒模式的下落速度抬到至少 Hard 基线，不再沿用过慢的 Normal 开局节奏。
  - 关闭主菜单 BGM，只保留游戏内成功 / 失败短音效，减少噪音感。
  - 为 Word 模式新增两个调节项：是否出现句子填空、出题方向（双向混合 / 英译中 / 中译英）。
  - 新增分学科错题本与 60 秒本地历史榜，均落地到 `localStorage`，主菜单 / 结算页可直接查看。
- 遇到了哪些问题：
  - 60 秒模式结束时，统计数据和结算渲染可能存在一帧延迟，直接写榜单会拿到旧正确率。
  - 错题本要想有价值，不能只记“错了”，还得带上原题、正解和用户当时的选择。
- 如何解决的：
  - 在 `App.tsx` 中增加统计 `ref`，确保计时结束那一刻写入榜单的是最新成绩快照。
  - 给 `GameEngine` 增加错题回传回调，在答错 / 漏题时把题面、正解、误选值或漏题状态回传给上层统一存储。

## 2026-03-28（Word 模式补强）

- 完成时间：2026-03-28 (UTC)
- 实现了哪些功能：
  - 为 `data/vocab.ts` 和 `data/cloze.ts` 增加难度分层，Word 模式不再只是“同一批词换个速度”。
  - 重写 `services/wordService.ts` 的题池筛选与补选项逻辑：先按难度取词，再在选项不足时用全局词库兜底补齐。
  - 将最高分拆分为 `brainRushHighScore_MATH` / `brainRushHighScore_WORD`，避免两种学科模式互相覆盖记录。
  - 菜单与结算文案补充 Word 难度说明，并把 60 秒模式文案改为更清晰的“计时模式”。
- 遇到了哪些问题：
  - 旧版本只有单一高分键，直接切换为双键会让历史数据丢失。
  - Word 题库规模还不大，若严格按难度过滤，容易把候选池抽空。
- 如何解决的：
  - 兼容读取旧的 `brainRushHighScore`，并按【预设】迁移给数学模式，避免已有成绩直接归零。
  - 采用“低难度向高难度递进放宽 + 全局词库兜底”的策略，在保留分层的同时保证题目可出、选项够用。

## 2026-03-26

- 完成时间：2026-03-26 (UTC)
- 实现了哪些功能：
  - 在主菜单新增“Math Rush / Word Speedrun”学科切换入口，Word 模式可直接开玩。
  - 新增 `wordService.ts`，实现三类题型随机：英文选中文、中文选英文、句子填空。
  - `GameEngine` 接入学科模式，按模式切换题目生成器而不改核心下落碰撞逻辑。
  - README 增加 Word Speedrun 玩法与本地词库数据策略说明。
- 遇到了哪些问题：
  - 原引擎直接耦合数学题生成函数，无法无痛扩展新题型。
  - 菜单存在“调参页仅数学有效”的交互差异，需避免 Word 模式出现无效参数入口。
- 如何解决的：
  - 给 `GameEngine` 增加 `subjectMode`，内部按模式调用 `mathService` 或 `wordService`。
  - 在菜单中仅对数学模式展示调参模块；Word 模式展示玩法说明卡片，避免误导。

## 2026-03-26（Word Speedrun 纠偏）

- 完成时间：2026-03-26 (UTC)
- 实现了哪些功能：
  - 将 Word 数据与逻辑解耦：新增 `data/vocab.ts`、`data/cloze.ts`、`utils/random.ts`。
  - 重写 `services/wordService.ts` 的出题逻辑，改为 `filter + shuffle + slice`，杜绝原 `while` 补选项的死循环风险。
  - 为完形填空增加“题目专属同类干扰项”，不再从全局词库随机抽离谱选项。
  - 统一改为 Fisher-Yates 洗牌，替换 `sort(() => Math.random() - 0.5)`。
- 遇到了哪些问题：
  - 原实现把完形干扰项和词汇干扰项混用，导致题目质量不稳定。
  - 词库不足时原补选项逻辑会出现无限循环隐患。
- 如何解决的：
  - 在 `types.ts` 增加 `ClozeItem.distractors` 类型约束，保证完形题天然可控。
  - 在 `wordService` 中对候选池先过滤再截断，池子不够时按现有数量返回，不再阻塞循环。

## 2026-03-26（入口可见性修复）

- 完成时间：2026-03-26 (UTC)
- 实现了哪些功能：
  - 将 Word/Math 切换入口改为主菜单卡片左上角固定按钮，保证首屏可见。
  - 删除中部重复切换区域，避免用户误以为没有入口。
- 遇到了哪些问题：
  - 原入口位于标题下方，视觉上不够“入口化”，在不同设备上容易被忽略。
- 如何解决的：
  - 把入口前置为主菜单卡片绝对定位按钮组，并保持中英文字短标签，降低识别成本。

## 2026-03-27（分支对齐）

- 完成时间：2026-03-27 (UTC)
- 将当前工作分支统一重命名为 `main`，避免继续在临时分支上累积提交。
